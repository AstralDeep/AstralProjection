name: android-ci

# Additive, independent of the backend Principle XI gates. Scoped to the
# android-client/ tree so backend changes don't trigger an Android build.
on:
  push:
    branches: [main]
    paths:
      - 'android-client/**'
      - 'contracts/ui_protocol.json'
      - 'contracts/fixtures/voice_065/client_conformance.json'
      - 'contracts/fixtures/voice_075/client_local_conformance.json'
      - 'scripts/run_android_next_major_canary.py'
      - 'tests/test_android_next_major_canary.py'
      - '.github/workflows/android-ci.yml'
  pull_request:
    paths:
      - 'android-client/**'
      - 'contracts/ui_protocol.json'
      - 'contracts/fixtures/voice_065/client_conformance.json'
      - 'contracts/fixtures/voice_075/client_local_conformance.json'
      - 'scripts/run_android_next_major_canary.py'
      - 'tests/test_android_next_major_canary.py'
      - '.github/workflows/android-ci.yml'
  schedule:
    - cron: '0 7 * * *' # nightly — runs the instrumented emulator job
  workflow_dispatch: {}

permissions:
  contents: read

# A new push to the same PR branch cancels the previous run (incl. the 45m
# emulator lane) instead of running both to completion; pushes to main and
# the nightly schedule never cancel each other.
concurrency:
  group: android-ci-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    working-directory: android-client

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: '17'

      # Provides Gradle on PATH (the committed wrapper works locally, but
      # setup-gradle gives CI caching + provenance).
      # The version is READ FROM the committed wrapper properties — the single
      # source of truth — so CI can never drift from the repo toolchain again
      # (a hard-pinned 8.11.1 broke the AGP 9.2.1 bump: AGP 9 needs Gradle ≥9.4.1).
      - name: Resolve Gradle version from the committed wrapper
        id: wrapper
        run: |
          v=$(sed -n 's/^distributionUrl=.*gradle-\([0-9][0-9.]*\)-\(bin\|all\)\.zip$/\1/p' gradle/wrapper/gradle-wrapper.properties)
          test -n "$v" || { echo "could not parse Gradle version from wrapper properties" >&2; exit 1; }
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a # v4.4.3
        with:
          gradle-version: ${{ steps.wrapper.outputs.version }}

      - name: Lint (ktlint + Android Lint)
        run: ./gradlew ktlintCheck :app:lintDebug --no-daemon --stacktrace

      - name: Unit tests (:core + :app JVM)
        run: ./gradlew :core:test :app:testDebugUnitTest --no-daemon --stacktrace

      - name: Coverage gate (Kover ≥ 90% on :core pure logic)
        run: ./gradlew :core:koverVerify --no-daemon --stacktrace

      - name: Publish app and core Kover XML inputs
        run: ./gradlew :app:koverXmlReport :core:koverXmlReport --no-daemon --stacktrace

      - name: Archive app and core Kover XML inputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: android-kover-xml
          path: |
            android-client/app/build/reports/kover/report.xml
            android-client/core/build/reports/kover/report.xml
          if-no-files-found: error
          retention-days: 14

      - name: Assemble debug APK
        run: ./gradlew :app:assembleDebug --no-daemon --stacktrace

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: app-debug-apk
          path: android-client/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: warn

  next-major-readiness:
    name: Android stable next-major readiness (isolated)
    # Non-required network-touching diagnostic — nightly (and manual) only,
    # not on every PR push.
    if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: '17'

      - name: Verify stable-release readiness without activating prereleases
        working-directory: .
        run: >-
          python3 scripts/run_android_next_major_canary.py
          android-client/gradle/next-major-canary.properties
          --allow-unreleased
          --verify-official-availability
          --output build/060/android-next-major-readiness.json

      - name: Archive stable-release readiness diagnostic
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: android-next-major-readiness
          path: build/060/android-next-major-readiness.json
          if-no-files-found: error

  # Instrumented Compose UI tests are a required Feature 065 client-conformance
  # gate. They run for the same path-filtered pull requests as the JVM suites,
  # as well as the existing nightly and manual invocations.
  instrumented:
    name: Required · instrumented UI tests (emulator)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: '17'

      # Same single-source-of-truth resolution as build-test (see above).
      - name: Resolve Gradle version from the committed wrapper
        id: wrapper
        run: |
          v=$(sed -n 's/^distributionUrl=.*gradle-\([0-9][0-9.]*\)-\(bin\|all\)\.zip$/\1/p' gradle/wrapper/gradle-wrapper.properties)
          test -n "$v" || { echo "could not parse Gradle version from wrapper properties" >&2; exit 1; }
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a # v4.4.3
        with:
          gradle-version: ${{ steps.wrapper.outputs.version }}

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Instrumented tests (:app:connectedDebugAndroidTest)
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        with:
          api-level: 34
          arch: x86_64
          working-directory: android-client
          script: ./gradlew :app:connectedDebugAndroidTest --no-daemon --stacktrace

  android-required:
    name: Required · Android aggregate
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs:
      - build-test
      - instrumented
    steps:
      - name: Enforce every Android conformance and coverage producer
        working-directory: .
        run: |
          [[ '${{ needs.build-test.result }}' == 'success' ]]
          [[ '${{ needs.instrumented.result }}' == 'success' ]]
