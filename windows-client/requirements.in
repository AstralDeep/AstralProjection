# Direct inputs for the Windows/Python 3.11 release environment.
#
# The complete transitive set and wheel hashes live in
# requirements-release.lock.txt.  Version changes here are deliberate release
# changes and must regenerate that lock on a clean Windows-compatible resolver.
PySide6==6.11.1
websockets==16.0
aiohttp==3.14.1
psutil==7.2.2
pyperclip==1.11.0
sigstore==3.6.7
astralprims==0.3.0
pyinstaller==6.21.0
livekit==1.1.14
# Public protected-executor contracts only; the independently deployed warden
# remains outside the Windows client and AstralProjection runtime.
lets-agent @ https://github.com/AstralDeep/LETS/releases/download/v1.0.10/lets_agent-1.0.10-py3-none-any.whl
