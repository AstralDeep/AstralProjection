"""AstralDeep Windows tools agent — a client-hosted A2A agent that exposes
Windows-specific tools (system info, clipboard, notifications, open path, list
directory) to the orchestrator. Runs on the user's Windows machine so the tools
execute locally; registered with the orchestrator over the standard A2A path."""
