<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Feature 053 — Mac App Store export options for the macOS archive.

  Separate from the iOS options because the macOS App Store export produces a
  `.pkg` rather than an `.ipa`, and because the macOS app needs its own
  provisioning profile: a `.mobileprovision` is per bundle-id AND platform, so
  the shared `com.personalailabs.astraldeep` bundle id still requires one profile
  for iOS and a different one for macOS.

  The macOS archive carries NO watch app — the "Embed Watch Content" phase is
  platform-filtered to iOS — so no watch profile appears here.

  TEMPLATE: the APPLE_TEAM_ID / APPLE_PROFILE_MACOS placeholders are substituted
  at release time from CI secrets by Scripts/render_export_options.py.
-->
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>
	<key>teamID</key>
	<string>${APPLE_TEAM_ID}</string>
	<key>signingStyle</key>
	<string>manual</string>
	<!-- The Mac App Store .pkg is signed by a SECOND identity, distinct from the
	     Apple Distribution certificate that signs the .app. -->
	<key>installerSigningCertificate</key>
	<string>3rd Party Mac Developer Installer</string>
	<key>provisioningProfiles</key>
	<dict>
		<key>com.personalailabs.astraldeep</key>
		<string>${APPLE_PROFILE_MACOS}</string>
	</dict>
	<key>manageAppVersionAndBuildNumber</key>
	<false/>
	<key>uploadSymbols</key>
	<true/>
	<key>destination</key>
	<string>export</string>
</dict>
</plist>
